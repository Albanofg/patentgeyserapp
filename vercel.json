{
  "buildCommand": "npm run build:client && esbuild server-entry/vercel.ts --platform=node --target=node20 --packages=external --bundle --format=esm --outfile=api/index.js --alias:@shared=./shared --banner:js=\"import{createRequire as __cr}from'module';const require=__cr(import.meta.url);\"",
  "outputDirectory": "dist/public",
  "installCommand": "npm install",
  "framework": null,
  "functions": {
    "api/index.js": {
      "maxDuration": 300
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
