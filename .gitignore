node_modules
dist
.DS_Store
Thumbs.db
server/public
api/index.js.map
vite.config.ts.*
*.tar.gz

# Secrets — keep .env.example tracked (it's the template)
.env
.env.local
.env.*.local

# Database dumps
*.sql
# …but keep migration files tracked. They're required by the schema-probe
# at server boot; if a deploy is missing them, the server refuses to start.
!migrations/*.sql

# Replit
.replit
replit.md

# Dev docs & assets
attached_assets
design_guidelines.md
docs
AGENT_MIGRATION.md

# External prompt-authoring reference (LEAP design pattern from Nitwyt).
# Kept alongside the app as authoring material for qa-assistant.md and the
# other module prompts; not part of this codebase.
leap-design-pattern/

# Claude
.claude
.local

# Local tool / config caches (npm, agent state, etc.)
.config
.agents

# Editor / IDE local settings
.vscode/
.idea/
*.swp
*.swo

# Build & lint caches
*.tsbuildinfo
.eslintcache
.cache/
coverage/

# Log files
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Documentation images
image/

# Admin / migration scripts (keep post-merge.sh tracked)
scripts/*
!scripts/post-merge.sh